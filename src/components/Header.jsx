const Header = ({ links }) => {
  return (
    <header className="py-[10px] px-[20px]">
      <img
        src="./images/logo.JPG"
        alt="logo_anb"
        className="w-[115px] h-[45px] object-contain"
      />
    </header>
  );
};

export default Header;
